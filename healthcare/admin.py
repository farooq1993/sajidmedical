from django.contrib import admin
#from healthcare.models.master_model import UserMaster
# from healthcare.models.master_model import User
# # Register your models here.

# class AdminUser(admin.ModelAdmin):
#     list_display = ('user_name', 'user_type', 'first_name', 'last_name', 'email_id', 'is_active')

# # Register the User model with the custom AdminUser class
# admin.site.register(User, AdminUser)